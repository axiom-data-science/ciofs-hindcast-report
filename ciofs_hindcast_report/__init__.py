
import pathlib
base = pathlib.Path(__path__[0])

PATH_INPUTS = base / "inputs"
PATH_INPUTS_RIVER = base / "inputs" / "river"
PATH_OUTPUTS = base / "outputs"
PATH_OUTPUTS_RIVER = PATH_OUTPUTS / "river"
